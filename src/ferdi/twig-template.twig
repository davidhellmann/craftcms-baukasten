{#
  @package <%= projectName %>
  @author <%= author.name %> [<%= author.email %>]

  <%= pathOptions.key.charAt(0) %>-<%= moduleName.replace('-', '_') %>
  ------------------------------------------------------------
#}

{# Include Variant #}
{% include '_<%= pathOptions.key %>/<%= moduleName %>/variant-' ~ opt.variant | default('basic') ~ '/_template.twig' with {
  opt: opt ??? null
} only %}
