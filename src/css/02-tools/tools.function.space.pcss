/*
 |--------------------------------------------------------------------------
 |  Space
 |--------------------------------------------------------------------------
 */

@function space($i) {
  @return ($bk-gutter * $i);
}

// Shorthand Version
@function s($i) {
  @return space($i);
}

