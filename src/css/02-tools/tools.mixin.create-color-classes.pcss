// https://codepen.io/jakealbaugh/post/css4-variables-and-sass
@mixin createColorClasses($map, $namespace) {
  @each $key, $value in $map {
    // maps require a second loop
    @if type-of($value) == 'map' {

      // each item in sub map
      @each $subKey, $subValue in $value {
        @if $subKey == 'base' {
          .#{$namespace}-#{$key} {
            color: var(--#{$namespace}-#{$key})
          }
          .bg-#{$key} {
            background-color: var(--#{$namespace}-#{$key})
          }
        } @else {
          .#{$namespace}-#{$key}-#{$subKey} {
            color: var(--#{$namespace}-#{$key}-#{$subKey})
          }
          .bg-#{$key}-#{$subKey} {
            background-color: var(--#{$namespace}-#{$key}-#{$subKey})
          }
        }
      }

    } @else {
      // top-level sizes
      .#{$namespace}-#{$key} {
        color: var(--#{$namespace}-#{$key})
      }
      .bg-#{$key} {
        background-color: var(--#{$namespace}-#{$key})
      }
    }
  }
}

@include createColorClasses($colors, 'c');
