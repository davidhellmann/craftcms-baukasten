{#
  c-definitionList
  ------------------------------------------------------------
#}

{# Set Defaults #}
{% set defaults = {
  cn: 'c-definitionList',
  modifiers: [],
  customClasses: [],
  data: {},
  items: null
} %}

{# Merge Default with Options #}
{% set props = props is defined ? defaults|merge(props) : defaults %}

{# Macros #}
{% import '_macros/_macro-settings.twig' as module %}





{# Modul #}
{% if props.items %}
  <dl {{ module.settings(props) }} waypoint>
    {% for item in props.items %}
      <dt class="{{ props.cn ~ '__term' }}  is-fadeInUp" waypoint-target>
        {{ item.term | raw }}
      </dt>
      <dd class="{{ props.cn ~ '__description' }}  is-fadeInUp" waypoint-target>
        <p>{{ item.description | nl2br }}</p>
      </dd>
    {% endfor %}
  </dl>
{% endif %}
