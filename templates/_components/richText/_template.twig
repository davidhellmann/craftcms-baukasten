{#
  a-richText
  ------------------------------------------------------------
#}

{% include '_atoms/richText/_template-' ~ opt.variant | default('basic') ~ '.twig' with {
  opt: opt ??? null
} only %}
