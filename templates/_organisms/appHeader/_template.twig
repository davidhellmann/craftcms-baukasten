{#
  o-appHeader
  ------------------------------------------------------------
#}

{# Set Defaults #}
{% set defaults = {
  cn: 'o-appHeader',
  modifiers: [],
  customClasses: ['section'],
  data: {},
  variant: 'basic'
} %}

{# Merge Default with Options #}
{% set opt = opt is defined ? defaults|merge(opt) : defaults %}

{# Modul #}
{# Include Variant #}
{% include [
  '_organisms/appHeader/variant-' ~ opt.variant ~ '/_template.twig',
] %}

