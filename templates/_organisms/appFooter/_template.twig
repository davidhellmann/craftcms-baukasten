{#
  o-appFooter
  ------------------------------------------------------------
#}

{# Include Variant #}
{% include '_organisms/appFooter/_template-' ~ opt.variant | default('basic') ~ '.twig' with {
  opt: opt ??? null
} only %}

