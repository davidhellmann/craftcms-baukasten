{#
  o-appFooter
  ------------------------------------------------------------
#}

{# Include Variant #}
{% include '_organisms/appFooter/variant-' ~ opt.variant | default('basic') ~ '/_template.twig' with {
  opt: opt ??? null
} only %}

