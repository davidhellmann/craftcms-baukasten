{% include '_organisms/section/_template.twig' with {
  props: {
    modifiers: [
      'cb',
      data.paddingVertical ?? null,
      data.width ?? null,
      data.background ?? null
    ],
    closetags: false
  }
} only %}
