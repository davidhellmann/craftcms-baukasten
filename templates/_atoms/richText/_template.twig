{#
  a-richText
  ------------------------------------------------------------
#}

{% include '_atoms/richText/variant-' ~ opt.variant | default('basic') ~ '/_template.twig' with {
  opt: opt ??? null
} only %}
