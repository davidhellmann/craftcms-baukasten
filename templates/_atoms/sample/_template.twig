{#
  @package craft-baukasten
  @author David Hellmann [david@hellmann.io]

  a-sample
  ------------------------------------------------------------
#}

{# -- Set Defaults -- #}
{% set defaults = {
  cn: 'a-sample',
  modifiers: [],
  customClasses: [],
  data: {},
  variant: 'basic',
  foo: null
} %}

{# -- Merge Default with Options -- #}
{% set opt = opt is defined ? defaults|merge(opt) : defaults %}

{# -- Macros -- #}
{% import '_macros/_macro-settings.twig' as module %}

{# -- Modul -- #}
{% if opt.foo %}
  {# Include Variant #}
  {% include [
    '_atoms/sample/variant-' ~ opt.variant ~ '/_template.twig',
    ] %}
{% endif %}

