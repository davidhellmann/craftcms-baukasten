{#
  a-headline
  ------------------------------------------------------------
#}

{# Include Variant #}
{% include '_atoms/headline/variant-' ~ opt.variant | default('basic') ~ '/_template.twig' with {
  opt: opt ??? null
} only %}
