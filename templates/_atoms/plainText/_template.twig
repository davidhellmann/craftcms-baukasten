{#
  a-plainText
  ------------------------------------------------------------
#}

{% include '_atoms/plainText/variant-' ~ opt.variant | default('basic') ~ '/_template.twig' with {
  opt: opt ??? null
} only %}
