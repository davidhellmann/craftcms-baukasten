{#
  a-plainText
  ------------------------------------------------------------
#}

{% include '_atoms/plainText/_template-' ~ opt.variant | default('basic') ~ '.twig' with {
  opt: opt ??? null
} only %}
