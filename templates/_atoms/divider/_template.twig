{#
  a-divider
  ------------------------------------------------------------
#}

{# Include Variant #}
{% include '_atoms/divider/variant-' ~ opt.variant | default('basic') ~ '/_template.twig' with {
  opt: opt ??? null
} only %}
