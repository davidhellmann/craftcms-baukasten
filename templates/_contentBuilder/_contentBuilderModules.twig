{# Content Builder Modules #}
{# Loop over all block and choose the right one #}
{# data has all the block data included #}

{% switch data.type %}
  {# Accordion #}
{% case 'hero' %}
  {% include '_builders/_includes/_cbHero.twig' with {
    data: data
  } only %}

  {# Accordion #}
{% case 'accordion' %}
  {% include '_builders/_includes/_cbAccordion.twig' with {
    data: data
  } only %}

  {# Section #}
{% case 'bkSection' %}
  {% include '_builders/_includes/_cbBksection.twig' with {
    data: data
  } only %}

  {# Code #}
{% case 'code' %}
  {% include '_builders/_includes/_cbCode.twig' with {
    data: data
  } only %}

  {# Definition List #}
{% case 'definitionList' %}
  {% include '_builders/_includes/_cbDefinitionList.twig' with {
    data: data
  } only %}

  {# Divider #}
{% case 'divider' %}
  {% include '_builders/_includes/_cbDivider.twig' with {
    data: data
  } only %}

  {# Image Cover #}
{% case 'imageCover' %}
  {% include '_builders/_includes/_cbImageCover.twig' with {
    data: data
  } only %}

  {# Image Gallery #}
{% case 'imageGallery' %}
  {% include '_builders/_includes/_cbImageGallery.twig' with {
    data: data
  } only %}

  {# Image Grid #}
{% case 'imageGrid' %}
  {% include '_builders/_includes/_cbImageGrid.twig' with {
    data: data
  } only %}

  {# Image Single #}
{% case 'imageSingle' %}
  {% include '_builders/_includes/_cbImageSingle.twig' with {
    data: data
  } only %}

  {# Image Slider #}
{% case 'imageSlider' %}
  {% include '_builders/_includes/_cbImageSlider.twig' with {
    data: data
  } only %}

  {# Quote #}
{% case 'quote' %}
  {% include '_builders/_includes/_cbQuote.twig' with {
    data: data
  } only %}

  {# Rich Text #}
{% case 'richText' %}
  {% include '_builders/_includes/_cbRichtText.twig' with {
    data: data
  } only %}

  {# Spacer#}
{% case 'spacer' %}
  {% include '_builders/_includes/_cbSpacer.twig' with {
    data: data
  } only %}

  {# Youtube #}
{% case 'youtube' %}
  {% include '_builders/_includes/_cbYoutube.twig' with {
    data: data
  } only %}

{% endswitch %}



