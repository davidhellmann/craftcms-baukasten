{% include '_organisms/imageGallery/_template.twig' with {
  opt: {
    modifiers: [
      'cb'
    ],
    customClasses: ['test'],
    images: data.images.all() ?? null,
    ratio: data.ratio ?? null,
  }
} only %}
