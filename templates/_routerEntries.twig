{# Entry Router #}
{# ---------------------------------------------------------------------- #}

{{ include([
  'entry/' ~ entry.section.handle ~ '/' ~ entry.type.handle,
  'entry/' ~ entry.section.handle ~ '/default',
  'entry/' ~ entry.section.handle ~ '/router',
  'errors/404',
  'errors/offline',
], {}, withContext = true) }}
