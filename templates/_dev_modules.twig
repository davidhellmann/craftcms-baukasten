<h1 class="">Tailwind Party.</h1>

{# ============================================================== #}
{#                                                                #}
{#      SEPERATOR                                                 #}
{#                                                                #}
{# ============================================================== #}
<pre class="mt-4 mb-2">_components/imageRaw</pre>
{# -- link -- #}
{% include '_components/image/imageRaw.twig' with {
  props: {
    image: craft.assets.title('Placeholder').volume('templateImages').one(),
    ratio: 'landscape'
  }
} only %}

{# ============================================================== #}
{#                                                                #}
{#      SEPERATOR                                                 #}
{#                                                                #}
{# ============================================================== #}
<pre class="mt-4 mb-2">_components/image</pre>
{# -- link -- #}
{% include '_components/image/image.twig' with {
  props: {
    image: craft.assets.title('Placeholder').volume('templateImages').one(),
    ratio: 'landscape'
  }
} only %}

{# ============================================================== #}
{#                                                                #}
{#      SEPERATOR                                                 #}
{#                                                                #}
{# ============================================================== #}
<pre class="mt-4 mb-2">_components/link</pre>
{# -- link -- #}
{% include '_components/link/link.twig' with {
  props: {
    url: 'https://davidhellmann.com',
    text: 'Click me'
  }
} only %}

{# ============================================================== #}
{#                                                                #}
{#      SEPERATOR                                                 #}
{#                                                                #}
{# ============================================================== #}
<pre class="mt-4 mb-2">_components/code</pre>
{# -- code -- #}
{% include '_components/code/code.twig' with {
  props: {
    variant: 'basic',
    snippet: "code[class*='language-'],
pre[class*='language-'] {
  color: #f8f8f2;
  tab-size: 4;
  hyphens: none;
}",
    language: 'scss'
  }
} only %}

