

<div class="section {{ data.variant.value == 'cover' ? 'h-screen' : ''}}">
  {# image #}
  {% include '_elements/image/picture.twig' with {
    props: {
      image: data.image.one() ??? null,
      objectFit: 'cover',
      ratio: 'square',
    }
  } only %}
  <div class="l-container">
    {{ data.headline }}
  </div>
</div>
