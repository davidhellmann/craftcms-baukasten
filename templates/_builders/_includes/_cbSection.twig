{# TODO: Handle Sections FIRST / Close Tags #}
{% include '_components/_layout/section/section.twig' with {
  props: {
    modifiers: [
      builder,
      data.paddingVertical ??? null,
      data.width ??? null,
      data.background ??? null
    ],
    closetags: false
  }
} only %}
