{% extends "_layouts/layout-default.twig" %}
{% set templatePath = _self ??? '' %}

{% block blockAppContent %}
  <div data-store='{"page": {{ craft.app.request.pageNum ?? 1 }}}'>
    {{ include('_components/datastar/paginateNews', {
      'limit': 12,
      'section': 'news',
      'entryUri': entry.uri ?? null,
      'headline': entry.customTitle ?? entry.title ?? null,
    }) }}
  </div>
{% endblock %}
